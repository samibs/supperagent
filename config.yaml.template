# Configuration for the AgentOS Platform
# --------------------------------------
#
# To use this file:
# 1. Make a copy of it and name it `config.yaml`.
# 2. Fill in your actual API keys and desired model names below.
# 3. IMPORTANT: The `config.yaml` file is ignored by git to protect your secrets.
#    Do NOT commit your `config.yaml` file to version control.

# API Keys for the different LLM providers
# You only need to fill in the keys for the services you intend to use.
api_keys:
  anthropic: "sk-ant-..."  # Your API key for Claude models
  google: "..."           # Your API key for Gemini models
  openai: "sk-..."          # Your API key for Codex/GPT models

# Model names to be used by the agents.
# You can specify the exact model versions you want to use.
# These are just examples; you can use any model available via your API key.
models:
  # Recommended for creative, planning, and documentation tasks.
  claude: "claude-3-haiku-20240307"

  # Recommended for analytical, logical reasoning, and code review tasks.
  gemini: "gemini-1.5-pro-latest"

  # Recommended for pure code generation tasks.
  codex: "gpt-4-turbo" # Using a modern equivalent for the "codex" role